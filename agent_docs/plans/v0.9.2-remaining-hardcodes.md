# Plan: v0.9.2 — Remaining Hardcode Fixes

## Status

PENDING — identified by full code audit on 2026-02-25.

## Goal

Fix all remaining hardcoded formatting values in the renderer and parser
that were identified during the comprehensive audit. After this, the only
non-LaTeX-driven values in the codebase should be documented fallback defaults
in `RenderProfile::from_layout()`.

## Findings from Audit

### Renderer — HIGH priority

| # | Lines | Issue | Fix |
|---|-------|-------|-----|
| 1 | ~2220-2224 | TOC chapter entries unconditionally uppercased | Gate on `profile.heading_uppercase` |
| 2 | ~2169, 2192 | TOC chapter prefix unconditionally uppercased | Gate on `profile.heading_uppercase` |

### Renderer — MEDIUM priority

| # | Lines | Issue | Fix |
|---|-------|-------|-----|
| 3 | ~1201 | Page number header alignment hardcoded `Center` | New `DocumentLayout` field + `RenderProfile` field |
| 4 | ~1041+ | Body text alignment hardcoded `Both` | New `DocumentLayout` field + `RenderProfile` field |
| 5 | ~1102 | Caption label style hardcoded to bold | Extract from `\captionsetup{font=...}` |
| 6 | ~2072-2074 | TOC depth hardcoded to 2 | Extract `\setcounter{tocdepth}{N}` |

### Parser — MEDIUM priority

| # | Lines | Issue | Fix |
|---|-------|-------|-----|
| 7 | ~6256, 6389 | Plain-text chapter heading detection Russian-only | Gate on `document_language`, use `default_chapter_name_for_language()` |

### Renderer — LOW priority (defer to v1.0)

| # | Lines | Issue | Fix |
|---|-------|-------|-----|
| 8 | ~2232, 2360 | Hyperlink color hardcoded `"000000"` | New `RenderProfile` field |
| 9 | ~2232, 2360 | Hyperlink underline hardcoded `"none"` | New `RenderProfile` field |

## Implementation Order

1. **Items 1-2** (HIGH): TOC uppercase — simple `if profile.heading_uppercase` gate.
   No new model fields needed.
2. **Item 6** (MEDIUM): TOC depth — new `DocumentLayout.toc_depth` + parser extraction.
3. **Item 5** (MEDIUM): Caption bold — extend `captionsetup` parser to extract `font` key.
4. **Items 3-4** (MEDIUM): Body/page-number alignment — new `DocumentLayout` fields.
5. **Item 7** (MEDIUM): Plain-text heading language gate — pass `document_language` through.

## Files to Change

- `crates/ferritex-core/src/parser/latex.rs` — items 3, 4, 5, 6, 7
- `crates/ferritex-core/src/model/mod.rs` — items 3, 4, 5, 6
- `crates/ferritex-renderer-docx/src/lib.rs` — items 1-6
- `crates/ferritex-core/tests/unit/parser_latex_tests.rs` — paired tests
- `crates/ferritex-renderer-docx/tests/unit/renderer_docx_tests.rs` — paired tests
- `AGENTS.md` — update parameter table
- `docs/SUPPORTED_ELEMENTS.md` — add new entries

## Quality Gate

```bash
cargo fmt --all
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```
