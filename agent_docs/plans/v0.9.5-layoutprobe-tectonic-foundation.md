# Plan: v0.9.5 LayoutProbe (embedded tectonic) Foundation

## Status

PARTIALLY COMPLETED (updated 2026-02-26).

Progress update (2026-02-26):
- Implemented in code:
  - `LayoutProbeOutput` contract in `ferritex-core` model.
  - `layout_probe` module with deterministic merge helper.
  - Feature-gated embedded `tectonic` probe backend (`layout-probe-tectonic`).
  - Parser wiring: probe execution + merge before final `Document` output.
  - Tests: unit + integration + property-style for precedence/determinism invariants.
  - Merge precedence refactored to parser-first (`parser > probe > fallback`) across parser/model/tests.
- Infra/workflow alignment completed for this stream:
  - Rust toolchain pinned (`rust-toolchain.toml`),
  - CI synchronized with local toolchain and lockfile checks,
  - CI includes probe feature build check and native dependency install step.
- Local validation passed:
  - `cargo fmt --all`
  - `cargo clippy --workspace --all-targets --locked -- -D warnings`
  - `cargo test --workspace --locked`
  - `cargo check -p ferritex-core --features layout-probe-tectonic --locked`
- Activation/visibility update (2026-02-25):
  - root `ferritex` crate now enables `layout-probe-tectonic` by default via feature pass-through wiring,
  - build start now logs active probe mode (`tectonic` vs parser-only),
  - parser logs warning when probe backend fails and falls back to parser-only extraction,
  - parser-only reproducible mode preserved via `--no-default-features`.
- Runtime cleanup update (2026-02-25):
  - removed external `pdfinfo` subprocess dependency from parser metadata collection,
  - `TotPages` now resolves from `.aux` sidecar signals (`\@abspage@last` / `\newlabel{LastPage}`),
  - added regression tests for both AUX extraction patterns and metadata-driven placeholder replacement.
- Probe recovery + external-corpus validation update (2026-02-25):
  - probe execution now enables `UnstableOptions { continue_on_errors: true }` to avoid aborting on recoverable TeX errors,
  - when TeX still errors, probe now applies degraded-mode field-level confidence filtering:
    - keep structural geometry/list signals,
    - selectively downgrade typography-sensitive probe fields (`font_size_body_hp`, `body_line_spacing_twips`) and defer downgraded fields to parser extraction,
  - fatal/no-signal failures still emit concise `.log` excerpt for diagnosis,
  - representative multi-file LaTeX corpus run now executes in probe-enabled mode with:
    - `LayoutProbe backend 'tectonic' extracted 16 field(s)` after safety filter,
    - no parser-only fallback warning,
    - DOCX body spacing restored from regressed `2.02` (`w:line=485`) to calibrated parser value (`w:line=332`).
- Runtime integration update (2026-02-25):
  - canonical PDF backend path is now wired via tectonic driver APIs in `ferritex-renderer-pdf`,
  - integration test validates `build --format pdf` end-to-end artifact generation,
  - first `tectonic::TexEngine` runtime integration slice is added for probe profile alignment
    (`halt_on_error`, `shell_escape`, `build_date`) with `ProcessingSessionBuilder`,
  - `TotPages` resolution is hardened beyond `.aux`:
    - sidecar layering now supports `.aux` + `.log`,
    - runtime fallback can infer total pages from canonical tectonic PDF bytes,
    - explicit-break heuristic now includes `\\newpage`, `\\clearpage`, `\\cleardoublepage`, `\\pagebreak`.
- Low-level pass orchestration update (2026-02-26):
  - probe execution now uses explicit pass plans under `TexEngine` runtime profile:
    - primary pass: `PassSetting::Tex` (single-pass marker extraction),
    - conditional recovery pass: `PassSetting::Default` with fixed `reruns=1`,
  - recovery pass triggers only when the primary pass errors or yields zero extracted fields,
  - selection policy prefers recovery output only when it improves signal quality and/or clears run-error status.
  - PDF diagnostics hardening update (2026-02-26):
  - PDF renderer now captures tectonic external-tool diagnostics instead of returning opaque `exit code` errors.
  - error reporting now includes concise external-tool stdout/stderr excerpts and biber/BCF compatibility hinting.
  - fail-fast guidance now explicitly points to runtime override controls:
    - CLI: `--pdf-biber-bin-dir <DIR>`,
    - env: `FERRITEX_PDF_BIBER_BIN_DIR`,
    - extra auto-candidate env: `FERRITEX_PDF_BIBER_BIN_DIRS`.
  - PDF bibliography runtime now supports explicit resolution policy:
    - `--pdf-biber-mode strict` → first-candidate fail-fast,
    - `--pdf-biber-mode auto` (default) → retry with alternative `biber` candidates on BCF mismatch.
  - PDF auto-mode now includes autonomous compatible-biber bootstrap:
    - when no local candidate matches observed BCF, ferritex can auto-download a pinned compatible biber binary into cache and retry the same build session,
    - controls:
      - disable bootstrap: `FERRITEX_PDF_BIBER_AUTO_INSTALL=0`,
      - cache root override: `FERRITEX_PDF_BIBER_CACHE_DIR=<DIR>`.
  - build-core external tool policy is now wired for all backends:
    - CLI: `--tool-install-policy ask|auto|never` (default `ask`),
    - policy contract flows through `build` orchestration and is consumed by PDF runtime installer logic.
  - biber bootstrap now honors policy:
    - `ask`: interactive confirmation before install; non-interactive runs fail-fast with guidance,
    - `auto`: install automatically on mismatch/local exhaustion,
    - `never`: fail-fast with manual-install + rerun guidance.
  - integration regression coverage added:
    - synthetic `biblatex` fixture,
    - fake `biber` override path,
    - assertion on fail-fast guidance text,
    - auto-mode recovery test (first fake biber mismatch, second fake biber succeeds).
  - representative corpus failure is now explicit:
    - `Found biblatex control file version 3.8, expected version 3.11`.
- Remaining to close plan exit criteria:
  - monitor `TotPages` runtime fallback behavior on larger multi-file corpora and add additional regressions if new edge cases appear,
  - close PDF bibliography-tool compatibility gap for multi-file `biblatex` projects under canonical tectonic runtime,
  - optional helper-crate evaluation (`biblatex`, `codebook-tree-sitter-latex`) only if new measured gaps appear.

## Goal

Maximize cross-project LaTeX compatibility by reducing renderer fallbacks to true edge cases.
Primary strategy:

```text
LaTeX source -> LayoutProbe (engine-evaluated effective values) + parser static extraction
             -> merge (parser > probe > fallback)
             -> AST + DocumentLayout
             -> RenderProfile::from_layout()
             -> renderer
```

Canonical backend direction:
- PDF parity path: tectonic driver runtime in `ferritex-renderer-pdf`.
- DOCX/MD path: parser-first semantics + probe fallback/validation.

## Dependency Policy (approved candidates)

Primary candidate:
- `tectonic` `0.15.0` (MIT): embedded TeX/LaTeX engine for effective layout/style probing.

Optional supporting candidates:
- `codebook-tree-sitter-latex` `0.6.1` (MIT): syntax indexing only (not semantic evaluation).
- `biblatex` `0.11.0` (MIT/Apache-2.0): bibliography parsing/evaluation support.

Not allowed for MIT core integration:
- `tex_engine` `0.1.8` (GPL-3.0-or-later),
- `rustex_lib` `0.1.7` (GPL-3.0-or-later).

## Non-negotiable constraints

1. No corpus-specific hardcodes.
2. No external LaTeX subprocess orchestration in runtime path for probe extraction.
3. Probe results must be normalized into generic `DocumentLayout` fields.
4. Renderer defaults remain fallback-only.

## Implementation Workstreams

### 1) Core probe contract in `ferritex-core`

- Introduce `LayoutProbeOutput` (internal normalized probe schema).
- Define deterministic merge contract into `DocumentLayout`:
  - parser static extraction is authoritative when present,
  - probe fills parser-absent fields,
  - renderer fallback only when both are absent.
- Keep merge idempotent and side-effect free.

### 2) Embedded `tectonic` spike (minimal vertical slice)

- Integrate `tectonic` as optional feature-gated dependency in `ferritex-core`.
- Build minimal probe pipeline for high-impact fields first:
  - body font size/family,
  - paragraph indent,
  - line spacing,
  - list geometry (`labelsep`, `labelwidth`, `leftmargin`, `itemindent`, `listparindent`),
  - page margins / paper size / gutter.
- Capture probe outputs in a sidecar-like structure consumable by parser/model code.

### 3) Parser/probe merge wiring

- Add merge step before final `Document` return in parse entrypoints.
- Ensure no behavior regression when probe is disabled/unavailable.
- Preserve existing parser behavior as deterministic fallback path.
- Add explicit enablement strategy for regular CLI usage (not only feature-check in CI),
  and add deterministic verification of active probe mode in validation workflow.

### 4) Bibliography and syntax helpers (optional stream)

- Evaluate `biblatex` integration for bibliography semantic gaps.
- Evaluate `codebook-tree-sitter-latex` where syntactic disambiguation improves extraction reliability.
- Add only if measurable extraction signal improves and complexity remains bounded.

### 5) Test strategy for probe integration

- Unit tests:
  - probe output normalization,
  - merge precedence (`parser > probe > fallback`),
  - absent-probe deterministic fallback behavior.
- Integration tests:
  - representative multi-file fixtures,
  - regression checks for previously fixed DOCX parity issues.
- Property-based tests (aligned with v0.9.4):
  - merge determinism,
  - monotonic length conversion invariants,
  - no-panics under partial probe/parser data.

### 6) Runtime tectonic reuse completion (in progress)

- ✅ Canonical PDF runtime integration via tectonic driver APIs in backend orchestration.
- ✅ `tectonic::TexEngine` integration now covers runtime profile alignment and direct low-level pass orchestration for probe runs.
- ✅ `TotPages` fragility reduced with layered extraction:
  - `.aux` and `.log` sidecar parsing,
  - runtime fallback via tectonic-generated PDF page-count inference,
  - stronger explicit-break heuristic for sidecar-absent paths.
- ✅ PDF fail-fast diagnostics now include actionable guidance and configurable biber override path for users with multiple local biber versions.
- ✅ PDF runtime supports both explicit fail-fast (`strict`) and transparent retry (`auto`) bibliography resolution under tectonic orchestration.
- ✅ PDF runtime now supports autonomous biber bootstrap path (no separate user-side biber installation step on supported platforms).
- ✅ PDF bootstrap flow now supports explicit user-facing install policy (`ask|auto|never`) with non-interactive guidance behavior.
- Remaining: monitor pass-selection behavior on larger multi-file corpora and adjust recovery thresholds only if measured gaps appear.

## Deliverables

1. `ferritex-core` probe contract + merge implementation (feature-gated initial version).
2. Initial `tectonic`-backed extraction for selected high-impact fields.
3. Updated docs:
   - `AGENTS.md`,
   - `agent_docs/latex_driven_policy.md`,
   - `docs/ARCHITECTURE.md`,
   - `docs/ROADMAP.md`,
   - `docs/SUPPORTED_ELEMENTS.md`.
4. Test coverage proving precedence and fallback minimization.
5. Runtime design/implementation note for tectonic driver + `TexEngine` integration boundaries.

## Exit Criteria

1. Probe integration works on representative LaTeX projects without external LaTeX subprocess orchestration.
2. For covered fields, renderer fallback usage is demonstrably reduced.
3. Parser-first precedence is enforced and tested (`parser > probe > fallback`).
4. Canonical PDF runtime path is implemented and documented with bounded next steps.
5. `TexEngine` integration path and `TotPages` fallback strategy are explicitly captured.
6. No regressions in workspace quality gate.
7. Documentation and session memory are synchronized to the new architecture.
